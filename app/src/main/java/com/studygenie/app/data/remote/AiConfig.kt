package com.studygenie.app.data.remote

/**
 * Configuration for AI Module.
 * Note: GEMINI_API_KEY is now managed via local.properties and BuildConfig.
 */
object AiConfig {
    // This file is now primarily for non-sensitive AI configuration constants if needed.
}
